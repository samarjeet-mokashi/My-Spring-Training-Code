package com.boa.service;

import java.util.List;

import org.springframework.stereotype.Service;

import com.boa.entity.Admin;

@Service
public class GradeService {

	
	/*
	 * public List<Admin> getAllAdmins() { return adminRepository.findAll(); }
	 * 
	 * public Admin addAdmin( Admin Admin) { return adminRepository.save(Admin); }
	 * 
	 * public Admin getAdminById( Long id) { return
	 * adminRepository.findById(id).orElse(null); }
	 * 
	 * public Admin updateAdmin( Long id, Admin updatedAdmin) { if
	 * (adminRepository.existsById(id)) { updatedAdmin.setId(id); return
	 * adminRepository.save(updatedAdmin); } return null; }
	 * 
	 * public void deleteAdmin( Long id) { adminRepository.deleteById(id); }
	 */
}
